import {StyleSheet} from 'react-native';

export const styleMapViewScreen = StyleSheet.create({
  container: {
    flex: 1,
  },

  container2: {
    flex: 0.93,
    backgroundColor: '#ffffff',
  },
});
